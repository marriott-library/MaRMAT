"""
marmat_web

Flask application package for the web frontend of MaRMAT (Marriott Reparative
Metadata Assessment Tool).

The app is designed to run with the ``marmat_web`` directory on ``sys.path`` so
that ``core`` and ``web`` are importable as top-level packages. Use
``app.py`` (the launcher) or import :func:`server.create_app`.
"""
